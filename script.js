/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////// DOM SELECTION ///////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


    const austinWeather = document.getElementById('austinWeather')
    const peopleInSpace = document.getElementById('peopleInSpace')
    const musicGenreStory = document.getElementById('musicGenreStory')

/////////////////////// CALLING THE API /////////////////////////////

    // Calling the Url.

fetch('http://api.openweathermap.org/data/2.5/weather?q=Austin,US&units=metric&APPID=efd0845f5916e3c871d91fde63e9b949')

    // Saying we want the response in json

    .then((response) => {
        return response.json()
 })

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////// THIS IS THE WEATHER AT THE TOP ///////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

     // First of is the forcastRightNow-part

.then((json) => {

    austinWeather.innerHTML = 
    `<h1>
    ${json.name} Texas |
    ${json.weather[0].main} |
    ${(Math.round(json.main.temp))}°C
    </h1>`

})

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////// NASA ////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


fetch('http://api.open-notify.org/astros.json')

    .then((response) => {
        return response.json()
 })

    .then((json) => {
    
    const numberofpeople = json.number
    peopleInSpace.innerHTML = `
    <h1>${numberofpeople} People are in space</h1>`
})

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////  ////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

fetch('https://binaryjazz.us/wp-json/genrenator/v1/story/')

    .then((response) => {
        return response.json()
 })

    .then((json) => {
    
    const story = json
    musicGenreStory.innerHTML = `
    <h1>${story}</h1>`
})
